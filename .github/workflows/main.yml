name: CI Pipeline

on:
  pull_request:
    branches:
      - devel
      - stage
      - prod

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
